# File: .github/workflows/coderabbit.yml
name: CodeRabbit

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  issue_comment:
    types: [created]

jobs:
  coderabbit:
    if: >
      (github.event_name == 'pull_request'
        && github.event.pull_request.draft == false
        && !github.event.pull_request.head.repo.fork) ||
      (github.event_name == 'issue_comment'
        && github.event.issue.pull_request != null
        && contains(github.event.comment.body, '@coderabbitai')
        && (github.event.comment.author_association == 'MEMBER'
          || github.event.comment.author_association == 'OWNER'
          || github.event.comment.author_association == 'COLLABORATOR'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: CodeRabbit
        # Pin to a stable tag or, preferably, a commit SHA
        uses: coderabbitai/ai-pr-reviewer@latest
        # uses: coderabbitai/ai-pr-reviewer@<commit-sha>
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}      - name: CodeRabbit
        uses: coderabbitai/ai-pr-reviewer@v1
        # uses: coderabbitai/ai-pr-reviewer@<commit-sha>
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}